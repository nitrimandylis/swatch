{
  "name": "swatch",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "bin": {
    "swatch": "./swatch.ts"
  },
  "scripts": {
    "compile": "bun build --compile swatch.ts --outfile ~/.bun/bin/swatch && mkdir -p $(brew --prefix)/share/man/man1 && cp man/swatch.1 $(brew --prefix)/share/man/man1/ && ([ -d ~/.claude/skills ] && cp -R swatch-cli ~/.claude/skills/ || true)"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
