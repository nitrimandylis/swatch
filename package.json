{
  "name": "rice",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "bin": {
    "rice": "./rice.ts"
  },
  "scripts": {
    "compile": "bun build --compile rice.ts --define process.env.RICE_BAKED_HOME=\"\\\"$PWD\\\"\" --outfile ~/.bun/bin/rice && mkdir -p $(brew --prefix)/share/man/man1 && cp man/rice.1 $(brew --prefix)/share/man/man1/"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
